language: node_js
node_js:
  - "8"
cache: npm
jobs:
  include:
    - stage: deploy
      script:
        - npm install -g firebase-tools
        - npm run build
        - firebase deploy --token "$FIREBASE_TOKEN"